import Foundation
import Alamofire
import Combine


extension ESI {
	public var {variable}: {scope} {
        return {scope}(esi: self, route: .path("{name}", next: .root(ESI.apiURL)))
	}

    {routes}
}
